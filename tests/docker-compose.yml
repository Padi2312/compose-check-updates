version: "3.8"

services:
  nginx:
    image: nginx:1.26.2

  nginx-latest:
    image: nginx:latest

  redis:
    image: redis:7.0.5

  postgres:
    image: postgres:14.5

  alpine:
    image: alpine:3.16

  pytorch-latest:
    image: pytorch/pytorch:latest

  pytorch:
    image: pytorch/pytorch:1.10.0
